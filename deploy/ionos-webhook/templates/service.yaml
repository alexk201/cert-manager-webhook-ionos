apiVersion: v1
kind: Service
metadata:
  name: {{ include "ionos-webhook.fullname" . }}
  labels:
    app: {{ include "ionos-webhook.name" . }}
    chart: {{ include "ionos-webhook.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    app: {{ include "ionos-webhook.name" . }}
    release: {{ .Release.Name }}
